import styled from 'styled-components';

export const Lists = styled.ul`
  height: 100%;
  list-style: none;
  padding: calc(72px + 1rem) 0 0 2rem;
  margin: 0;
  display: flex;
  overflow-x: auto;
`;
